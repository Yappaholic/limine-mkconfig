[package]
name = "limine-mkconfig"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = "4.6.0"
libc = { version = "0.2.183", features = ["extra_traits"] }

[build-dependencies]
bindgen = "0.72.1"
